using System;
using System.Collections.Generic;
using System.Text;

namespace Najm.Handler.SkyMap
{
    public class SkyDirection
    {/*
        SkyDirection(double ra, double dec)
        {
            InitializeSkyDirection(ra, dec, false);
        }
        SkyDirection(double coord1, double coord2, bool galactic)
        {
            InitializeSkyDirection(coord1, coord2, galactic);
        }
        SkyDirection(I3Vector equatorialVector)
        {
        }
        SkyDirection(I3Vector v, bool galactic)
        {
        }

        public double RA { get { return _coord1; } }
        public double DEC { get { return _coord2; } }
        public double L { get { return _coord1; } }
        public double B { get { return _coord2; } }
        public bool Valid { get { return false; } }

        public double AngleTo(SkyDirection dir)
        {
            return 0;
        }

        private void InitializeSkyDirection(double coord1, double coord2, bool galactic)
        {
        }
        private void ConvertToGalactic()
        {
        }

        private double _coord1;
        private double _coord2;
      * */
    }
}
